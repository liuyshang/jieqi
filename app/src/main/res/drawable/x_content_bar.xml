<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 背景图 -->
    <item
        android:id="@+id/background"
        android:drawable="@drawable/content_progress" />

    <!-- 进度度 -->
    <item
        android:id="@+id/progress"
        android:drawable="@drawable/content_progress" />
</layer-list>